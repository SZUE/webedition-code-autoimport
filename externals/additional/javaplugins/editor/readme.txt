Build the java template editor applet:
========================================

you need the SUN Java SDK to build the applet.
Enter the following commands:

javac Editor.java
jar cvf editor.jar *.class

this will create the editor.jar that has to be installed 
into the directory
  /webEdition/editor/